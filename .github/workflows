name: Example Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run a script
      run: echo Hello, world!
    - name: Use GitHub Token
      run: echo "Using GitHub Token ${{ secrets.PERSONAL_TOKEN }}"
